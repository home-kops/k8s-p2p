apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lidarr-ingress
  namespace: p2p
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - match: Host(`lidarr.${DOMAIN}`)
    kind: Rule
    services:
    - kind: Service
      name: lidarr-service
      port: web
