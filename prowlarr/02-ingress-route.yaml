apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prowlarr-ingress
  namespace: p2p
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - match: Host(`prowlarr.${DOMAIN}`)
    kind: Rule
    services:
    - kind: Service
      name: prowlarr-service
      port: web
