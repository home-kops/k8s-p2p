apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-ingress
  namespace: p2p
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - match: Host(`sonarr.${DOMAIN}`)
    kind: Rule
    services:
    - kind: Service
      name: sonarr-service
      port: web
